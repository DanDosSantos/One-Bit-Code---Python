nome = input('Digite o nome do contato: ')
numero = input('Digite o telefone do contato: ')
outro = input('Deseja continuar? [S]im [N]ão: ')

while True:
    if outro.lower() == 's':
            
        elif outro.lower() == 'n':
            break
        else:
            print('Opção Inválida')